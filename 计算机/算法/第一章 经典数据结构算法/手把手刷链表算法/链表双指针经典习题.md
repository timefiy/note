---
title: 链表双指针经典习题
source: https://labuladong.online/zh/algo/problem-set/linkedlist-two-pointers/
author:
  - "[[../../../../author/labuladong]]"
published:
created: 2026-02-17
description: 本文汇总了 力扣/LeetCode 上所有链表双指针算法的经典习题，包含 labuladong 思路讲解和算法可视化，支持 Java/C++/Python/Golang/JavaScript。
tags:
  - clippings
---
前置知识

阅读本文前，你需要先学习：

- [链表基础](https://labuladong.online/zh/algo/data-structure-basic/linkedlist-basic/)
- [单链表的双指针技巧汇总](https://labuladong.online/zh/algo/essential-technique/linked-list-skills-summary/)

## 链表的分解

Tip

链表的分解技巧可以运用到很多单链表题目中，题目并不一定明确地要求你把链表分解成两部分，只要要求你从链表筛选出若干节点，都可以用这个技巧。

### 82\. 删除排序链表中的重复元素 II

给定一个已排序的链表的头 `head` ， *删除原始链表中所有重复数字的节点，只留下不同的数字* 。返回 *已排序的链表* 。

**示例 1：**

![](https://labuladong.online/images/lc/uploads/2021/01/04/linkedlist1.jpg)
```
输入：head = [1,2,3,3,4,4,5]
输出：[1,2,5]
```

**示例 2：**

![](https://labuladong.online/images/lc/uploads/2021/01/04/linkedlist2.jpg)
```
输入：head = [1,1,1,2,3]
输出：[2,3]
```

**提示：**

- 链表中节点数目在范围 `[0, 300]` 内
- `-100 <= Node.val <= 100`
- 题目数据保证链表已经按升序 **排列**

题目来源： [力扣 82. 删除排序链表中的重复元素 II](https://leetcode.cn/problems/remove-duplicates-from-sorted-list-ii/) 。

#### 基本思路

这道题可以有多种解法，最简单粗暴的解法是用 [哈希集合](https://labuladong.online/zh/algo/data-structure-basic/hash-set/) 来记录重复节点，需要额外的空间复杂度，我们不讨论。下面探讨如何用双指针技巧，避免使用额外的空间复杂度来求解。

第一种思路，也是我比较推荐的方式，就是把这种题转化成 [链表的双指针技巧汇总](https://labuladong.online/zh/algo/essential-technique/linked-list-skills-summary/) 中讲的链表分解的技巧。题目其实就是让你把链表分解成「重复元素」和「不重复元素」两条链表，然后把不重复元素这条链表返回即可。

第二种思路，可以把这道题理解为 [链表的双指针技巧汇总](https://labuladong.online/zh/algo/essential-technique/linked-list-skills-summary/) 中讲的 [83\. 删除排序链表中的重复元素](https://leetcode.cn/problems/remove-duplicates-from-sorted-list/description/) 的变体，只不过 83 题让你把多于的重复元素去掉，这道题要求你把所有重复的元素全都去掉。

第三种思路，可以用递归思维来做，稍微难理解一些，我也写出来供大家参考。

#### 解法代码

```
// 推荐的通用解法，运用链表分解的技巧

class Solution {

public:

    ListNode* deleteDuplicates(ListNode* head) {

        // 将原链表分解为两条链表

        // 一条链表存放不重复的节点，另一条链表存放重复的节点

        // 运用虚拟头结点技巧，题目说了 node.val <= 100，所以用 101 作为虚拟头结点

        ListNode dummyUniq(101);

        ListNode dummyDup(101);

        ListNode* pUniq = &dummyUniq;

        ListNode* pDup = &dummyDup;

        ListNode* p = head;

        while (p != nullptr) {

            if ((p->next != nullptr && p->val == p->next->val) || p->val == pDup->val) {

                // 发现重复节点，接到重复链表后面

                pDup->next = p;

                pDup = pDup->next;

            } else {

                // 不是重复节点，接到不重复链表后面

                pUniq->next = p;

                pUniq = pUniq->next;

            }

            p = p->next;

            // 将原链表和新链表断开

            pUniq->next = nullptr;

            pDup->next = nullptr;

        }

        return dummyUniq.next;

    }

};

// 快慢双指针解法

class Solution2 {

public:

    ListNode* deleteDuplicates(ListNode* head) {

        ListNode dummy(-1);

        ListNode* p = &dummy;

        ListNode* q = head;

        while (q != nullptr) {

            if (q->next != nullptr && q->val == q->next->val) {

                // 发现重复节点，跳过这些重复节点

                while (q->next != nullptr && q->val == q->next->val) {

                    q = q->next;

                }

                q = q->next;

                // 此时 q 跳过了这一段重复元素

                if (q == nullptr) {

                    p->next = nullptr;

                }

                // 不过下一段元素也可能重复，等下一轮 while 循环判断

            } else {

                // 不是重复节点，接到 dummy 后面

                p->next = q;

                p = p->next;

                q = q->next;

            }

        }

        return dummy.next;

    }

};

// 递归解法

class Solution3 {

public:

    // 定义：输入一条单链表头结点，返回去重之后的单链表头结点

    ListNode* deleteDuplicates(ListNode* head) {

        // base case

        if (head == nullptr || head->next == nullptr) {

            return head;

        }

        if (head->val != head->next->val) {

            // 如果头结点和身后节点的值不同，则对之后的链表去重即可

            head->next = deleteDuplicates(head->next);

            return head;

        }

        // 如果如果头结点和身后节点的值相同，则说明从 head 开始存在若干重复节点

        // 越过重复节点，找到 head 之后那个不重复的节点

        while (head->next != nullptr && head->val == head->next->val) {

            head = head->next;

        }

        // 直接返回那个不重复节点开头的链表的去重结果，就把重复节点删掉了

        return deleteDuplicates(head->next);

    }

};
```

#### 算法可视化

算法可视化

## 链表的合并

Tip

有些题目虽然不是链表的题目，但其中蕴含了合并有序链表的思想。

### 378\. 有序矩阵中第 K 小的元素

**378\. 有序矩阵中第 K 小的元素** | [力扣](https://leetcode.cn/problems/kth-smallest-element-in-a-sorted-matrix/) | [LeetCode](https://leetcode.com/problems/kth-smallest-element-in-a-sorted-matrix/)

给你一个 `n x n` 矩阵 `matrix` ，其中每行和每列元素均按升序排序，找到矩阵中第 `k` 小的元素。  
请注意，它是 **排序后** 的第 `k` 小元素，而不是第 `k` 个 **不同** 的元素。

你必须找到一个内存复杂度优于 `O(n<sup>2</sup>)` 的解决方案。

**示例 1：**

```
输入：matrix = [[1,5,9],[10,11,13],[12,13,15]], k = 8
输出：13
解释：矩阵中的元素为 [1,5,9,10,11,12,13,13,15]，第 8 小元素是 13
```

**示例 2：**

```
输入：matrix = [[-5]], k = 1
输出：-5
```

**提示：**

- `n == matrix.length`
- `n == matrix[i].length`
- `1 <= n <= 300`
- `-10<sup>9</sup> <= matrix[i][j] <= 10<sup>9</sup>`
- 题目数据 **保证** `matrix` 中的所有行和列都按 **非递减顺序** 排列
- `1 <= k <= n<sup>2</sup>`

**进阶：**

- 你能否用一个恒定的内存(即 `O(1)` 内存复杂度)来解决这个问题?
- 你能在 `O(n)` 的时间复杂度下解决这个问题吗?这个方法对于面试来说可能太超前了，但是你会发现阅读这篇文章（ [this paper](http://www.cse.yorku.ca/~andy/pubs/X+Y.pdf) ）很有趣。

题目来源： [力扣 378. 有序矩阵中第 K 小的元素](https://leetcode.cn/problems/kth-smallest-element-in-a-sorted-matrix/) 。

#### 基本思路

这道题其实是前文 [单链表的六大解题套路](https://labuladong.online/zh/algo/essential-technique/linked-list-skills-summary/) 中讲过的 [23\. 合并K个升序链表](https://leetcode.cn/problems/merge-k-sorted-lists/description/) 的变体。

矩阵中的每一行都是排好序的，就好比多条有序链表，你用优先级队列施展合并多条有序链表的逻辑就能找到第 `k` 小的元素了。

#### 解法代码

```
#include <queue>

#include <vector>

class Solution {

public:

    int kthSmallest(std::vector<std::vector<int>>& matrix, int k) {

        // 存储二元组 (matrix[i][j], i, j)

        // i, j 记录当前元素的索引位置，用于生成下一个节点

        auto cmp =  {

            // 按照元素大小升序排序

            return a[0] > b[0];

        };

        std::priority_queue<std::vector<int>, std::vector<std::vector<int>>, decltype(cmp)> pq(cmp);

        // 初始化优先级队列，把每一行的第一个元素装进去

        for (int i = 0; i < matrix.size(); i++) {

            pq.push({matrix[i][0], i, 0});

        }

        int res = -1;

        // 执行合并多个有序链表的逻辑，找到第 k 小的元素

        while (!pq.empty() && k > 0) {

            std::vector<int> cur = pq.top();

            pq.pop();

            res = cur[0];

            k--;

            // 链表中的下一个节点加入优先级队列

            int i = cur[1], j = cur[2];

            if (j + 1 < matrix[i].size()) {

                pq.push({matrix[i][j + 1], i, j + 1});

            }

        }

        return res;

    }

};
```

#### 算法可视化

算法可视化

### 373\. 查找和最小的 K 对数字

**373\. 查找和最小的 K 对数字** | [力扣](https://leetcode.cn/problems/find-k-pairs-with-smallest-sums/) | [LeetCode](https://leetcode.com/problems/find-k-pairs-with-smallest-sums/)

给定两个以 **非递减顺序排列** 的整数数组 `nums1` 和 `nums2`, 以及一个整数 `k` 。

定义一对值 `(u,v)` ，其中第一个元素来自 `nums1` ，第二个元素来自 `nums2` 。

请找到和最小的 `k` 个数对 `(u<sub>1</sub>,v<sub>1</sub>)`, ` (u<sub>2</sub>,v<sub>2</sub>)`... `(u<sub>k</sub>,v<sub>k</sub>)` 。

**示例 1:**

```
输入: nums1 = [1,7,11], nums2 = [2,4,6], k = 3
输出: [1,2],[1,4],[1,6]
解释: 返回序列中的前 3 对数：
     [1,2],[1,4],[1,6],[7,2],[7,4],[11,2],[7,6],[11,4],[11,6]
```

**示例 2:**

```
输入: nums1 = [1,1,2], nums2 = [1,2,3], k = 2
输出: [1,1],[1,1]
解释: 返回序列中的前 2 对数：
     [1,1],[1,1],[1,2],[2,1],[1,2],[2,2],[1,3],[1,3],[2,3]
```

**提示:**

- `1 <= nums1.length, nums2.length <= 10<sup>5</sup>`
- `-10<sup>9</sup> <= nums1[i], nums2[i] <= 10<sup>9</sup>`
- `nums1` 和 `nums2` 均为 **升序排列**
- `1 <= k <= 10<sup>4</sup>`
- `k <= nums1.length * nums2.length`

题目来源： [力扣 373. 查找和最小的 K 对数字](https://leetcode.cn/problems/find-k-pairs-with-smallest-sums/) 。

#### 基本思路

这道题其实是前文 [单链表的六大解题套路](https://labuladong.online/zh/algo/essential-technique/linked-list-skills-summary/) 中讲过的 [23\. 合并K个升序链表](https://leetcode.cn/problems/merge-k-sorted-lists/description/) 的变体。

怎么把这道题变成合并多个有序链表呢？就比如说题目输入的用例：

```
nums1 = [1,7,11], nums2 = [2,4,6]
```

组合出的所有数对儿这就可以抽象成三个有序链表：

```
[1, 2] -> [1, 4] -> [1, 6]

[7, 2] -> [7, 4] -> [7, 6]

[11, 2] -> [11, 4] -> [11, 6]
```

这三个链表中每个元素（数对之和）是递增的，所以就可以按照 [23\. 合并K个升序链表](https://leetcode.cn/problems/merge-k-sorted-lists/description/) 的思路来合并，取出前 `k` 个作为答案即可。

#### 解法代码

```
class Solution {

public:

    vector<vector<int>> kSmallestPairs(vector<int>& nums1, vector<int>& nums2, int k) {

        // 存储三元组 (num1[i], nums2[i], i)

        // i 记录 nums2 元素的索引位置，用于生成下一个节点

        auto cmp =  {

            // 按照数对的元素和升序排序

            return (a[0] + a[1]) > (b[0] + b[1]);

        };

        priority_queue<vector<int>, vector<vector<int>>, decltype(cmp)> pq(cmp);

        

        // 按照 23 题的逻辑初始化优先级队列

        for (int i = 0; i < nums1.size(); i++) {

            pq.push({nums1[i], nums2[0], 0});

        }

        vector<vector<int>> res;

        // 执行合并多个有序链表的逻辑

        while (!pq.empty() && k > 0) {

            vector<int> cur = pq.top();

            pq.pop();

            k--;

            // 链表中的下一个节点加入优先级队列

            int next_index = cur[2] + 1;

            if (next_index < nums2.size()) {

                pq.push({cur[0], nums2[next_index], next_index});

            }

            res.push_back({cur[0], cur[1]});

        }

        return res;

    }

};
```

#### 算法可视化

算法可视化

## 链表运算题

### 2\. 两数相加

**2\. 两数相加** | [力扣](https://leetcode.cn/problems/add-two-numbers/) | [LeetCode](https://leetcode.com/problems/add-two-numbers/)

给你两个 **非空** 的链表，表示两个非负的整数。它们每位数字都是按照 **逆序** 的方式存储的，并且每个节点只能存储 **一位** 数字。

请你将两个数相加，并以相同形式返回一个表示和的链表。

你可以假设除了数字 0 之外，这两个数都不会以 0 开头。

**示例 1：**

![](https://labuladong.online/images/lc/aliyun-lc-upload/uploads/2021/01/02/addtwonumber1.jpg)
```
输入：l1 = [2,4,3], l2 = [5,6,4]
输出：[7,0,8]
解释：342 + 465 = 807.
```

**示例 2：**

```
输入：l1 = [0], l2 = [0]
输出：[0]
```

**示例 3：**

```
输入：l1 = [9,9,9,9,9,9,9], l2 = [9,9,9,9]
输出：[8,9,9,9,0,0,0,1]
```

**提示：**

- 每个链表中的节点数在范围 `[1, 100]` 内
- `0 <= Node.val <= 9`
- 题目数据保证列表表示的数字不含前导零

题目来源： [力扣 2. 两数相加](https://leetcode.cn/problems/add-two-numbers/) 。

#### 基本思路

逆序存储很友好了，直接遍历链表就是从个位开始的，符合我们计算加法的习惯顺序。如果是正序存储，那倒要费点脑筋了，可能需要 [翻转链表](https://labuladong.online/zh/algo/data-structure/reverse-linked-list-recursion/) 或者使用栈来辅助。

这道题主要考察 [链表双指针技巧](https://labuladong.online/zh/algo/essential-technique/linked-list-skills-summary/) 和加法运算过程中对进位的处理。注意这个 `carry` 变量的处理，在我们手动模拟加法过程的时候会经常用到。

**代码中还用到一个链表的算法题中是很常见的「虚拟头结点」技巧，也就是 `dummy` 节点** 。你可以试试，如果不使用 `dummy` 虚拟节点，代码会稍显复杂，而有了 `dummy` 节点这个占位符，可以避免处理初始的空指针情况，降低代码的复杂性。

#### 解法代码

```
class Solution {

public:

    ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {

        // 在两条链表上的指针

        ListNode *p1 = l1, *p2 = l2;

        // 虚拟头结点（构建新链表时的常用技巧）

        ListNode *dummy = new ListNode(-1);

        // 指针 p 负责构建新链表

        ListNode *p = dummy;

        // 记录进位

        int carry = 0;

        // 开始执行加法，两条链表走完且没有进位时才能结束循环

        while (p1 != nullptr || p2 != nullptr || carry > 0) {

            // 先加上上次的进位

            int val = carry;

            if (p1 != nullptr) {

                val += p1->val;

                p1 = p1->next;

            }

            if (p2 != nullptr) {

                val += p2->val;

                p2 = p2->next;

            }

            // 处理进位情况

            carry = val / 10;

            val = val % 10;

            // 构建新节点

            p->next = new ListNode(val);

            p = p->next;

        }

        // 返回结果链表的头结点（去除虚拟头结点）

        return dummy->next;

    }

};
```

#### 算法可视化

算法可视化

### 445\. 两数相加 II

给你两个 **非空** 链表来代表两个非负整数。数字最高位位于链表开始位置。它们的每个节点只存储一位数字。将这两数相加会返回一个新的链表。

你可以假设除了数字 0 之外，这两个数字都不会以零开头。

**示例1：**

![](https://labuladong.online/images/lc/1626420025-fZfzMX-image.png)

```
输入：l1 = [7,2,4,3], l2 = [5,6,4]
输出：[7,8,0,7]
```

**示例2：**

```
输入：l1 = [2,4,3], l2 = [5,6,4]
输出：[8,0,7]
```

**示例3：**

```
输入：l1 = [0], l2 = [0]
输出：[0]
```

**提示：**

- 链表的长度范围为 ` [1, 100]`
- `0 <= node.val <= 9`
- 输入数据保证链表代表的数字无前导 0

**进阶：** 如果输入链表不能翻转该如何解决？

题目来源： [力扣 445. 两数相加 II](https://leetcode.cn/problems/add-two-numbers-ii/) 。

#### 基本思路

这道题是 [2\. 两数相加](https://leetcode.cn/problems/add-two-numbers/description/) 的进阶问题，我们模拟加法运算当然是从最低位开始加，这样才能正确的处理进位。但现在单链表的开头是最高位，那么最直接的想法就是先 [翻转链表](https://labuladong.online/zh/algo/data-structure/reverse-linked-list-recursion/) ，这样就可以继续玩第 2 题那一套了，没什么难度。

不过本题也说了，如果不让你反转链表怎么办？其实也好办，我们可以利用栈这种先进后出的数据结构，把链表节点从头到尾放进栈中，再从栈拿出来就是从尾到头的顺序，相当于是反转链表的效果，然后又回到了第 2 题的加法逻辑。

还有一个需要注意的是，计算结果的高位也应该放在结果链表的左侧，也就是插入到 `dummy` 节点的后面。具体看代码吧。

#### 解法代码

```
#include <stack>

class Solution {

public:

    ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {

        // 把链表元素转入栈中

        std::stack<int> stk1;

        while (l1 != nullptr) {

            stk1.push(l1->val);

            l1 = l1->next;

        }

        std::stack<int> stk2;

        while (l2 != nullptr) {

            stk2.push(l2->val);

            l2 = l2->next;

        }

        

        // 接下来基本上是复用我在第 2 题的代码逻辑

        // 注意新节点要直接插入到 dummy 后面

        // 虚拟头结点（构建新链表时的常用技巧）

        ListNode* dummy = new ListNode(-1);

        // 记录进位

        int carry = 0;

        // 开始执行加法，两条链表走完且没有进位时才能结束循环

        while (!stk1.empty() || !stk2.empty() || carry > 0) {

            // 先加上上次的进位

            int val = carry;

            if (!stk1.empty()) {

                val += stk1.top();

                stk1.pop();

            }

            if (!stk2.empty()) {

                val += stk2.top();

                stk2.pop();

            }

            // 处理进位情况

            carry = val / 10;

            val = val % 10;

            // 构建新节点，直接接在 dummy 后面

            ListNode* newNode = new ListNode(val);

            newNode->next = dummy->next;

            dummy->next = newNode;

        }

        // 返回结果链表的头结点（去除虚拟头结点）

        return dummy->next;

    }

};
```

#### 算法可视化

算法可视化

更新时间：2026/02/17 14:24

## 评论(78)

Markdown

Ctrl+Enter 发表

![阿多](https://thirdwx.qlogo.cn/mmopen/vi_32/Q0j4TwGTfTJjnr0MibW0P2tOE15jEw7X0l4kXShibAGxzMBPZSZLoqTyUHKfJz9SHhhBDymuz7KBwkQHR3A9HTcQ/132)

阿多 3 个月前

378 这道题目为什么写的这么复杂，既然二维数组是有序的，一次性将二位数组数据，全部塞入，然后逐个poll出来k个不行么？有空间浪费？ PriorityQueue<Integer> queue = new PriorityQueue<>((a, b) -> a - b); int rowLength = matrix.length; int colLength = matrix\[0\].length; for (int i = 0; i < rowLength; ++i) { for (int j = 0; j < colLength; ++j) { queue.offer(matrix\[i\]\[j\]); } }

```
int res = -1;

    int count = 0;

    while (count < k){

        res = queue.poll();

        count++;

    }

    return res;
```

![labuladong](https://thirdwx.qlogo.cn/mmopen/vi_32/Q0j4TwGTfTKUUrMeBCJjuvHKjkDGparuRmYns8MHsWeCZza0OZerEZqUmazBMQrNKb942Tmmiad0svicOiadib8eicQ/132)

labuladong

labuladong 东哥 20 天前

你说的方法可以得到正确答案，但空间复杂度是 O(n^2)。题目要求「内存复杂度优于 O(n^2)」，所以文章用优先队列只维护 n 个元素（每行一个），空间复杂度降到 O(n)。

![玊尔](https://thirdwx.qlogo.cn/mmopen/vi_32/EFUtJYJA7XxbYc3obSN7jFVMJLiccYPHdlr8iaA481elxORF0G0xAwKjvbojmrfA4cYSynY6ibxMFzudzdhARXShat69ySltS55xFTupjJQ75c/132)

玊尔 5 个月前

在矩阵中求最小K值的这题，我把矩阵用priority\_queue 进行排序，最后for loop 吐出第k个 内存 和 我用 priority 装tuple 的方式在leetcode 显示差不多，但是我感觉前者内存是 T(m+n) 后者 是 T(m) ，我的理解是不是对的啊

![labuladong](https://thirdwx.qlogo.cn/mmopen/vi_32/Q0j4TwGTfTKUUrMeBCJjuvHKjkDGparuRmYns8MHsWeCZza0OZerEZqUmazBMQrNKb942Tmmiad0svicOiadib8eicQ/132)

labuladong

labuladong 东哥 20 天前

如果把整个矩阵都放进优先队列，空间复杂度是 O(n^2)（n x n 矩阵）。文章的方法只维护 n 个元素（每行一个代表），空间复杂度是 O(n)。LeetCode 显示的内存差不多可能是因为测试用例规模较小。

![Enbin Yang](https://thirdwx.qlogo.cn/mmopen/vi_32/MhjHqURDEq7tUJ7mLohYTCAbvhgtnNmtoYdTSl4M8KUekqEQoonO6FF2eURibI8pI8Ae8aSe1TT6pmlAa3Yx8bt7Jlfb8xy8uVk9J0luZukA/132)

Enbin Yang

Enbin Yang 6 个月前

通过 pNuiq 链表和原始链表节点比较，在本地和可视化面板都是可以的，但是力扣上提交就出错，显示丢节点了，想请教下是什么原因？class Solution { public ListNode deleteDuplicates(ListNode head) { ListNode dummyUniq = new ListNode(101); ListNode dummyDup = new ListNode(101);

```
ListNode pUniq = dummyUniq;

    ListNode pDup = dummyDup;

    ListNode p = head;

    while (p != null) {

        // 重复元素

        if (pUniq.val != p.val) {

            pUniq.next = p;

            pUniq = pUniq.next;

        } else {

            pDup.next = p;

            pDup = pDup.next;

        }

        p = p.next;

        pUniq.next = null;

        pDup.next = null;

    }

    return dummyUniq.next;

}
```

}

![labuladong](https://thirdwx.qlogo.cn/mmopen/vi_32/Q0j4TwGTfTKUUrMeBCJjuvHKjkDGparuRmYns8MHsWeCZza0OZerEZqUmazBMQrNKb942Tmmiad0svicOiadib8eicQ/132)

labuladong

labuladong 东哥 20 天前

你的代码用 `pUniq.val != p.val` 判断是否重复，这只能处理「相邻重复」的情况（83 题）。但 82 题要求删除「所有出现过重复的元素」，比如 `[1,2,2,3]` 应该返回 `[1,3]` ，你的代码会返回 `[1,2,3]` 。文章的解法用 `pDup.val` 记录重复值，能正确处理这种情况。

![。。。](https://thirdwx.qlogo.cn/mmopen/vi_32/iaj85Pia2KvicsmP7E3ibTyTic5RVpiaLOia5splOrOZv9yrDXCyWOsIjKLRmwYCte3aBuaxghJCYe46c0DWibbOibccWyQ/132)

。。。 6 个月前

老师，82题的第二种思路，链表的双指针技巧汇总 现在貌似没有83. 删除排序链表中的重复元素 的讲解了

![labuladong](https://thirdwx.qlogo.cn/mmopen/vi_32/Q0j4TwGTfTKUUrMeBCJjuvHKjkDGparuRmYns8MHsWeCZza0OZerEZqUmazBMQrNKb942Tmmiad0svicOiadib8eicQ/132)

labuladong

labuladong 东哥 20 天前

83 题的讲解在 [双指针技巧](https://labuladong.online/zh/algo/essential-technique/array-two-pointers-summary/) 文章的「快慢指针去重」部分，那里用数组去重类比讲解了链表去重。

![0.5L](https://thirdwx.qlogo.cn/mmopen/vi_32/KU3mW0MNM07qfuVN0TS5SIpLCrfkvDwDJXpC0Zd9pl3PN9wCNZwsMn2ibVUSK4coHhVze7BfbJaP3ibxGUgOOmjd1XJsYzF80KYpKZBbZLalk/132)

0.5L 12 个月前

开头82的那道从排序链表中删除重复元素的通用解法是不是有bug？如果重复元素出现在最后的话,p.next==null，进不到重复的那个if分支吧

![labuladong](https://thirdwx.qlogo.cn/mmopen/vi_32/Q0j4TwGTfTKUUrMeBCJjuvHKjkDGparuRmYns8MHsWeCZza0OZerEZqUmazBMQrNKb942Tmmiad0svicOiadib8eicQ/132)

labuladong

labuladong 东哥 20 天前

代码是正确的。判断条件是 `(p.next != null && p.val == p.next.val) || p.val == pDup.val` ，用 `||` 连接。当重复元素在最后时，比如 `[1,2,3,3]` ，第一个 3 满足前半条件被接入 pDup；第二个 3 虽然 `p.next==null` ，但满足后半条件 `p.val == pDup.val` ，仍会被正确接入 pDup。

![柚子大王](https://thirdwx.qlogo.cn/mmopen/vi_32/petsz7bmadgs4WlFIicbUibicBfRqzlLGvQfZAjPIo5y4icdIoibNVWvtn53EMia6zdmHPZ6GQcsjpeBEWrOeian3huwqd0Xv3XwjYpibMCnBFGeeKk/132)

柚子大王 12 个月前

let pq = new MinPriorityQueue({ compare: (a, b) => { // 按照元素大小升序排序 return a\[0\] - b\[0\]; } }); 这个最小堆的方法在力扣中运行报错了

![labuladong](https://thirdwx.qlogo.cn/mmopen/vi_32/Q0j4TwGTfTKUUrMeBCJjuvHKjkDGparuRmYns8MHsWeCZza0OZerEZqUmazBMQrNKb942Tmmiad0svicOiadib8eicQ/132)

labuladong

labuladong 东哥 20 天前

LeetCode API 改变了，这个问题已经修复。

![少驭🐉](https://thirdwx.qlogo.cn/mmopen/vi_32/fSrDcvmb2VaONZLj4BulHuSbG1jQFVCvsIXJdzwzib4ic1wqRs4ib4F7ia9hmSkAuRuzqqpgyA1QWuVY8AFdhhcLpbiaKG0A8ibFERHocKjibwoVcE/132)

少驭🐉 超过 1 年前

264丑数这道题， 我的直觉写法是 product =2×p2，而不是ugly\[p2\] 然后两种解法都试了下，都是可以的，但是好没明白是为什么

![saibalmars](https://avatars.githubusercontent.com/u/4888846?v=4)

saibalmars

saibalmars 超过 1 年前

p2如果一直+1下去夠大的情況會出現2 3 5以外的質數，像是7 這樣就不符合醜數的定義了，所以得用ugly\[p2\]

![zoe](https://thirdwx.qlogo.cn/mmopen/vi_32/Q3auHgzwzM7BUEs0UbPfIHicQt6MJt6TqTqHKDd4qmQjunTDBmgnlibGmO0rpqia9V9NxHg4PbPRNuumEwU6MwrbejV9OEAP7lz/132)

zoe 超过 1 年前

当p2加到7,11,13...的时候，得出的14,22,26...就不是丑数了，所以要用ugly\[p2\]\*2，也就是丑数的2倍还是丑数。

E

Endeavour86 回复 @zoe 超过 1 年前

这个地方不太懂， 为什么 7\*2 就不是丑数了

![NR⁵²](https://thirdwx.qlogo.cn/mmopen/vi_32/ap93cqvlvtV8TzbOgnHcGOdad7O5Jo7fXlpUNibC9xBEWHJC26icl4w0sdst8ZicuByPE0QAVOInPhiavnawaGTsMJ6PJd8Kc5NkAOzzhrG35Bg/132)

NR⁵² 2 个月前

378 有点难理解😄 然后一步一步跟着东哥代码往下算

```
1     5     9

10  11    13

12  13    15

把每一行的第一个元素装进去

1， 0,  0

10，1, 0

12,   2，0

合并

第一次遍历 ,  此时k = 8

cur  = [1， 0,  0]

res = cur[0] = 1

k-- = 8-1    =  7

int i = cur[1]  =   0

j = cur[2]     =    0

if ( 1 < 3) {

    5, 0,1   添加进队列

}

此时队列为

1， 0,  0

5,    0,  1

10，1, 0

12,   2，0

第二次遍历 ,  此时k = 7

cur  = [5， 0,  1]

res = cur[0] = 5

k-- = 7-1    =  6

int i = cur[1]  =   0

j = cur[2]     =    1

if ( 2 < 3) {

    9, 0,2   添加进队列

}

此时队列为

1， 0,  0

5,    0,  1

9, 0,2

10，1, 0

12,   2，0
```

发现合并循环是把 `matrix` 的数组中的其它数据往进添加。 不得不佩服能想出来这种方式🥲

![kenan](https://thirdwx.qlogo.cn/mmopen/vi_32/Q3auHgzwzM4SW78hVqdkicm5icUWHzsqRfGfmNRt3ttHoyE16n50vkG8aZbKVicYlLXP1w3e24wgoibkSLz5THUmvibJU4umtfpeL/132)

kenan 将近 2 年前

1. 从未排序的链表中移除重复元素

---

感觉这样写比较容易理解

```
public ListNode deleteDuplicatesUnsorted(ListNode head) {

            HashMap<Integer, Integer> map = new HashMap<>();

            ListNode dummy = new ListNode(-1);

            ListNode q = head, p = dummy;

            while (q != null) {

                map.put(q.val, map.getOrDefault(q.val, 0) + 1);

                q = q.next;

            }

            q = head;

            while (q != null) {

                // 只把无重复的元素放进p(dummy)

                if (map.get(q.val) == 1) {

                    p.next = q;

                    p = p.next;

                }

                q = q.next; 

            }

            p.next = null; // 断开链表

            return dummy.next;

        }
```

![Zane](https://thirdwx.qlogo.cn/mmopen/vi_32/GvkkGphgWFiabTpZhHcsUMommp7lwu9vakdAJDL4o8mRogZU5ByT3OLKIHVhq9wFB6cxH1JmTsxrd5g0ksqrKm1jiaiaUoiaSw0DL5KUCJiaicdLg/132)

Zane 大约 2 个月前

445反转链表：

```
public ListNode reverse(ListNode head) {

        ListNode prev = null;

        ListNode cur = head;

        while (cur != null) {

            ListNode next = cur.next;

            cur.next = prev;

            prev = cur;

            cur = next;

        }

        return prev;

    }
```

![Zane](https://thirdwx.qlogo.cn/mmopen/vi_32/GvkkGphgWFiabTpZhHcsUMommp7lwu9vakdAJDL4o8mRogZU5ByT3OLKIHVhq9wFB6cxH1JmTsxrd5g0ksqrKm1jiaiaUoiaSw0DL5KUCJiaicdLg/132)

Zane 大约 2 个月前

445反转链表：

```
public ListNode reverse(ListNode head) {

        ListNode prev = null;

        ListNode cur = head;

        while (cur != null) {

            ListNode next = cur.next;

            cur.next = prev;

            prev = cur;

            cur = next;

        }

        return prev;

    }
```

![hao](https://thirdwx.qlogo.cn/mmopen/vi_32/Q3auHgzwzM6v78icribwGs9AICUL4yc24jaa8Ou93BzkcCyTibcrshcJBWRk7fFjI88vkNz4UOwvdicvpKNlMDzSvg/132)

hao 2 个月前

力扣373“查找和最小的 K 对数字”，用值域二分法，总是有测试用例通过不了。有时间再好好检查下。

![MrHedwig](https://thirdwx.qlogo.cn/mmopen/vi_32/PiajxSqBRaEIHHyicK9pkI0MH4icTgNqySMo9Yc5kXW9iaQltoed6gbdIdUH3qicOpmkcOfs6NZx1L1SehpqNzMibicvQR4IEJUsKKj1XsZ1oc6c8XHBhInpInKIg/132)

MrHedwig 2 个月前

445 两数相加II 不反转列表用 stack感觉是脑筋急转弯一样。。 因为既没有省时间(和反转数组相比)，也没有省空间。

主体算法结构也差不多，且stack 本身还可以是双链表或者 dynamic array, in python。

硬要说就是没有改变input?

![abb](https://thirdwx.qlogo.cn/mmopen/vi_32/AWBicuyscibia29ACGbRItPXD78Vjo1NlJGeaJGnzJiaIDURYlfCbFz9nOib1RFJ8u074bOpcRc2GvmnpjHDA3WWICVCbwuq8ExaB2icicm0lxHTOQ/132)

abb 8 个月前

网站能不能给做一个笔记功能，类似这个评论功能，每一页都可以记一下我的笔记？

![labuladongacount](https://avatars.githubusercontent.com/u/226433265?v=4)

labuladongacount

labuladongacount 8 个月前

两数相加这题我服了,操,在相加过程中完成了翻转,看了半天才看懂, 厉害啊,看来这反转我还没学到位啊,就离谱

![大DUANG](https://thirdwx.qlogo.cn/mmopen/vi_32/Q0j4TwGTfTJpUa0hjYy3D45lygxedvZtc5RhXDMIPzD5A5jQmJVDwPT7rX0c7Hro9M2MBtl3fzuIGyfPkwygHg/132)

大DUANG 超过 1 年前

本章节的前置知识：单链表的双指针技巧汇总 - 跳转到了（双指针技巧秒杀七道链表题目）。麻烦看看是不是跳转错误还是文章已经更新。谢谢

![labuladong](https://thirdwx.qlogo.cn/mmopen/vi_32/Q0j4TwGTfTKUUrMeBCJjuvHKjkDGparuRmYns8MHsWeCZza0OZerEZqUmazBMQrNKb942Tmmiad0svicOiadib8eicQ/132)

labuladong

labuladong 东哥 超过 1 年前

没有错误，就是这篇。

![阿飞](https://thirdwx.qlogo.cn/mmopen/vi_32/DYAIOgq83eobdib6tdOdf8NqV6Qo0EjMfMdp8uuKRd6Xoo06wH1Xe6myJleJQtHuLPVO9ZPFac74Ic7CHn1hLdg/132)

阿飞 超过 1 年前

```
class Solution {

    public ListNode deleteDuplicates(ListNode head) {

        // p0 永远在 p 前边一个位置

        ListNode dummy = new ListNode(-101), tail = dummy, p = head, p0 = new ListNode(-101, p);

        while (p!=null) {

            // 当前节点和前边的一个节点值不同

            if (p0.val!=p.val) {

                // 1. 如果后边没有节点了

                // 2. 如果还有后边的节点，并且和后边的节点的值也不同，这说明当前节点的值是没有重复的

                // 当前节点的下一个节点不为空

                if (p.next==null || (p.next!=null && p.val!=p.next.val)) {

                    tail.next = p;

                    tail = p;

                }

            }

            p0 = p0.next;

            p = p.next;

        }

        // 这里得断掉与后边的联系【卧槽，坑死啦】

        tail.next = null;

        return dummy.next;

    }

}
```

![雾散](https://thirdwx.qlogo.cn/mmopen/vi_32/PiajxSqBRaEKTboIkz89KLWBy8L55iaF7YDr7yibFSR8B0DdpW8taNmA10t5ARYVPeySdekckJic8gHiaia2fhziaOZ3m411brn0AFy77ekyuYsYUbiaJCUmvmD1Kg/132)

雾散 4 个月前

感觉这一章好多都是基于优先队列的东西 纯链表不太多

![Ca1m](https://thirdwx.qlogo.cn/mmopen/vi_32/JN0xmlQD3icica3WVicibGGL6nezGepoohibribQ7DKG8icxbspyBicj73swmgicd5bRRkaLPibKls74y8aWdGPJNlSaCicajAO8P5ZWJNHq8MzL6P1zQw/132)

Ca1m 4 个月前

删除重复链表 // 运用虚拟头结点技巧，题目说了 node.val <= 100，所以用 101 作为虚拟头结点 ListNode dummyUniq(101); ListNode dummyDup(101); 它这里和ListNode dummyUniq(-1)区别是什么，为什么-1就通不过

![阿伦](https://thirdwx.qlogo.cn/mmopen/vi_32/bdHF1mQyTCpTicqe00bOc8aHZc2qvRLMrpN5icIb9dibYDz0Y9hkcBicQmNvhgU8ufEPVYPY1eWuFoShicf9l9gRQASb12X3R2fV1VWEJPnweiaCQ/132)

阿伦 4 个月前

因为题目里给的条件是-100 <= Node.val <= 100

![Kiumb1223](https://avatars.githubusercontent.com/u/141305684?v=4)

Kiumb1223 10 个月前

在leetcode的python环境里面，queue这个包是不是没有呀？ 有序矩阵的第k个元素那里 ， 我导入from queue import PriorityQueue 提示 我没有这个queue这个包

![F](https://thirdwx.qlogo.cn/mmopen/vi_32/Q3auHgzwzM7gVaD3mtr1rgexk19KiaT49S1neSK5uCqIWicmSWq3ISuCFarOYUGa9iaTXYjVnQicDS7V8Jiaiby06gnyxloJu1tfv8/132)

F 8 个月前

换成 `Python3` 就有了